#To use a private Docker Hub image in Kubernetes, you need to create an Image Pull Secret and reference it in your deployment.

kubectl create secret docker-registry dockerhub-secret --docker-server=https://index.docker.io/v1/ --docker-username=daliabess51 --docker-password=<token>
